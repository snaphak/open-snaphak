# GitHub Pages (Jekyll) config for the Snapmap+ site.
# The site is site/ (deployed by .github/workflows/pages.yml): the HTML pages (index, changelog,
# 404) are served verbatim; the markdown pages (the guide, the privacy policy) are rendered
# through _layouts/ (their front matter picks the layout). One source for the guide.
title: Snapmap+
description: Snapmap+ opens up DOOM (2016) SnapMap — full property editing, no editor limits.
markdown: kramdown
kramdown:
  input: GFM
